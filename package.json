{
  "name": "ayursutra",
  "version": "1.0.0",
  "private": true,
  "description": "AyurSutra - Panchakarma Patient Management & Therapy Scheduling SaaS Platform",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "dev:api": "npm run dev --workspace=apps/api",
    "dev:web": "npm run dev --workspace=apps/web",
    "dev": "concurrently \"npm run dev:api\" \"npm run dev:web\"",
    "build:api": "npm run build --workspace=apps/api",
    "build:web": "npm run build --workspace=apps/web",
    "build": "npm run build --workspace=packages/shared-types && npm run build:api && npm run build:web"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
